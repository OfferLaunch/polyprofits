# Copy to .env and add your GitHub Personal Access Token so "push to github" in chat works automatically.
# Get a token: https://github.com/settings/tokens (classic, repo scope)
# Do not commit .env or share your token.
GITHUB_TOKEN=
